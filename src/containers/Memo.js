import React from 'react';
import { render } from 'react-dom';

class Memo extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>Memo.</div>
    );
  }
}

export default Memo;
